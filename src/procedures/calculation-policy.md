# Calculation policy

When the user asks for any arithmetic or a numeric result — totals, prices,
percentages, conversions — do NOT compute it in your head. Call the
`calculate` tool with the exact expression and use its result.

Language models are unreliable at multi-digit arithmetic; the tool is exact.
Always prefer the tool over a mental estimate, even for "easy" math.
