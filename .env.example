# Copy to .env and fill in. Never commit .env.

# Model provider (default seam: OpenAI)
OPENAI_API_KEY=your-api-key-here

# Optional: swap the default chat model
# MODEL=gpt-4o-mini

# Optional: swap the embedding model used by retrieval
# EMBED_MODEL=text-embedding-3-small
